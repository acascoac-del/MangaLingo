# MangaLingo API — requirements.txt
# Python 3.10 / 3.11 / 3.12 (NO 3.13 — torch CPU wheels no disponibles)
#
# Instalacion recomendada (Windows PowerShell):
#   py -3.12 -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# Si tenes GPU NVIDIA con CUDA, omite el flag --index-url en la linea de torch
# para que instale la version CUDA por defecto.

# ---------- Torch (instalar aparte con --index-url CPU) ----------
torch
torchvision

# ---------- ML / Transformers ----------
transformers>=4.40
huggingface_hub>=0.20
accelerate>=0.20
safetensors>=0.4
sentencepiece>=0.1.99
protobuf>=3.20,<6.0
timm>=0.9
kornia>=0.7
einops>=0.7
open_clip_torch>=2.24
ctranslate2>=4.0
onnxruntime>=1.17

# ---------- manga-ocr (kha-white) ----------
manga-ocr>=0.1.11

# ---------- Config / utils ----------
omegaconf>=2.3
pydantic>=2.0,<3.0
python-dotenv>=1.0
colorama>=0.4
rich>=13.0
loguru>=0.7
tqdm>=4.65
packaging>=23.0
psutil>=5.9

# ---------- Idiomas ----------
py3langid==0.2.2
langdetect>=1.0
langcodes[data]>=3.3

# ---------- CV / Image ----------
opencv-python-headless>=4.8
pillow>=10.0
shapely>=2.0
pyclipper>=1.3
scikit-image>=0.22
scipy>=1.11
freetype-py>=2.4
ImageHash>=4.3
albumentations>=1.3

# ---------- Text / Unicode ----------
python-bidi>=0.4
arabic-reshaper>=3.0
regex>=2023.5
editdistance>=0.8
pyhyphen>=4.0

# ---------- Network / HTTP ----------
requests>=2.31
httpx==0.27.2
aiohttp>=3.9
aioshutil>=1.5
aiofiles>=23.2
websockets>=12.0

# ---------- Data ----------
numpy==1.26.4
pandas>=2.0
networkx>=3.0

# ---------- API server ----------
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
starlette>=0.36
marshmallow>=3.20

# ---------- Translators (cada uno con sus deps) ----------
deepl>=1.30
openai==1.63.0
tiktoken>=0.6
groq>=1.0
google-genai>=0.3
googletrans>=4.0
pygoogletranslation>=2.0

# ---------- Optional (no fallan si no se instalan) ----------
# pydensecrf     # refinamiento de mascara CRF — opcional, difficile en Windows
# pytorch_lightning
# torchsummary
# xformers
# rusty_manga_image_translator  # paddle_rust detector — solo Linux
# pytesseract                   # fallback OCR ligero — solo si queres el backend OpenCV+Tesseract
